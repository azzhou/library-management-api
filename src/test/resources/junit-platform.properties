# defaults to instantiating test classes per test, which means setup/teardown functions need to be in companion object
# changing to per_class allows using regular functions for setup/teardown
junit.jupiter.testinstance.lifecycle.default = per_class